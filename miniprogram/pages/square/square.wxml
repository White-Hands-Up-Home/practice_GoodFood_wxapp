<!--pages/square/square.wxml-->
<view class="check">吃乎评价</view>

<scroll-view class="evaluation" scroll-y>
<view class="stores" wx:for="{{stores}}" wx:key="index"> 
  <view class="list" bindtap="goToDetail">
    {{item.name}}  {{item.price}}
  </view>
</view>
</scroll-view>
